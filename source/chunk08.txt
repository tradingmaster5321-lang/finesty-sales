label>Product<select id=retProduct>${db.products.map(p=>`<option value=${p.id}>${esc(p.name)}</option>`).join('')}</select></label><label>Qty<input id=retQty type=number min=1 value=1></label><label>Reason<input id=retReason></label><label>Refund method<select id=retPay><option>Cash</option><option>Original payment</option><option>Exchange</option></select></label></div><br><button class=primary onclick=processReturn()>Process Return</button></div><br><div class=card><h2>Return history</h2><table><tr><th>Date</th><th>Invoice</th><th>Product</th><th>Qty</th><th>Refund</th></tr>${db.returns.slice().sort(txSort).map(r=>`<tr><td>${fmtDate(r.date)}</td><td>${r.invoice}</td><td>${esc(r.product)}</td><td>${r.qty}</td><td>${money(r.refund)}</td></tr>`).join('')}</table></div>`}function processReturn(){let s=db.sales.find(x=>x.invoice===retInv.value.trim());if(!s)return alert('Invoice haijapatikana');let p=db.products.find(x=>x.id===retProduct.value),line=s.items.find(i=>i.id===p?.id);let q=+retQty.value;if(!p||!line||q<=0)return alert('Bidhaa/quantity si sahihi');let already=db.returns.filter(r=>r.invoice===s.invoice&&r.productId===p.id).reduce((a,r)=>a+Number(r.qty||0),0),remaining=Math.max(0,Number(line.qty||0)-already);if(q>remaining)return alert('Quantity ya return imezidi kiasi kilichouzwa. Iliyobaki kurudishwa: '+remaining);let refund=line.price*q,cost=line.cost*q;p.stock+=q;db.returns.push({id:id(),date:new Date().toISOString(),invoice:s.invoice,product:p.name,productId:p.id,qty:q,refund,cost,reason:retReason.value,payment:retPay.value,user:user.username});if(s.payment==='Credit'&&s.customerId){let c=db.customers.find(x=>x.id===s.customerId);if(c){c.balance=Math.max(0,(c.balance||0)-refund);c.transactions=c.transactions||[];c.transactions.push({date:new Date().toISOString(),type:'return_refund',amount:refund,ref:s.invoice})}}audit('RETURN',s.invoice+' '+p.name+' x'+q+' refund '+money(refund));save();toast('Return imehifadhiwa: stock, sales na profit vimesasishwa');go('returns')}
function reportFilter(){let mode=document.getElementById('reportMode')?.value||'all',from=document.getElementById('reportFrom')?.value||'',to=document.getElementById('reportTo')?.value||'';return {mode,from,to,ok:(iso)=>{let d=iso?new Intl.DateTimeFormat('en-CA',{timeZone:'Africa/Dar_es_Salaam'}).format(new Date(iso)):'';if(mode==='day')return !!from&&d===from;if(mode==='range')return !!from&&!!to&&d>=from&&d<=to;return true}}}
function filteredReportData(){let f=reportFilter(),sales=db.sales.filter(s=>f.ok(s.date)),returns=db.returns.filter(r=>f.ok(r.date)),expenses=db.expenses.filter(e=>f.ok(e.date)),purchases=db.purchases.filter(p=>f.ok(p.date));return {f,sales,returns,expenses,purchases}}
function reportTitle(f){return f.mode==='day'?`Siku Maalum: ${f.from}`:f.mode==='range'?`Date Range: ${f.from} hadi ${f.to}`:'Mauzo Yote / Total Information'}
function aggregateProducts(sales,returns){let m={};sales.forEach(s=>s.items.forEach(i=>{let k=i.id||i.name;if(!m[k])m[k]={name:i.name,sku:i.sku||'',unit:i.unit||'',qty:0,sales:0,cost:0,discount:0,invoiceCount:0,refund:0};m[k].qty+=Number(i.qty||0);m[k].sales+=Number(i.qty||0)*Number(i.price||0);m[k].cost+=Number(i.qty||0)*Number(i.cost||0);m[k].invoiceCount++}));returns.forEach(r=>{let k=r.productId||r.product;if(!m[k])m[k]={name:r.product,sku:'',unit:'',qty:0,sales:0,cost:0,discount:0,invoiceCount:0,refund:0};m[k].refund+=Number(r.refund||0);m[k].qty-=Number(r.qty||0);m[k].cost-=Number(r.cost||0)});return Object.values(m).map(x=>({...x,netSales:Math.max(0,x.sales-x.refund),netCost:Math.max(0,x.cost),profit:Math.max(0,x.sales-x.refund)-Math.max(0,x.cost)})).filter(x=>x.qty||x.sales||x.refund)}
function reportRows(data){return data.sales.slice().sort(txSort).map(s=>{let n=saleNet(s);return `<tr><td>${fmtDate(s.date)}</td><td>${s.invoice}</td><td>${esc(s.userName||s.user)}</td><td>${esc(s.customerName||'Walk-in')}</td><td>${esc(s.payment)}</td><td>${money(n.total)}</td><td>${money(n.total-n.cost)}</td></tr>`}).join('')||'<tr><td colspan=7 class=muted>Hakuna mauzo kwenye kipindi hiki.</td></tr>'}
function editPurchase(id0){if(!can(['Admin','Manager']))return alert('Admin/Manager pekee');let x=db.purchases.find(a=>a.id===id0);if(!x)return;let p=db.products.find(a=>a.id===x.productId);let ns=+prompt('Qty mpya',x.qty);if(!(ns>0))return;let nc=+prompt('Cost mpya / unit',x.cost);if(!(nc>=0))return;let np=ns-x.qty;if(p && p.stock+Math.min(0,np)<0)return alert('Stock haitoshi kubadilisha purchase hii');if(p)p.stock+=np;p.buy=nc;let oldTotal=x.total,oldBal=x.balance;x.qty=ns;x.cost=nc;x.total=ns*nc;x.paid=Math.min(x.total,x.paid);x.balance=Math.max(0,x.total-x.paid);if(x.supplierId){let sp=db.suppliers.find(a=>a.id===x.supplierId);if(sp)sp.balance=Math.max(0,(sp.balance||0)-oldBal+x.balance)}audit('EDIT_PURCHASE',x.product+' '+money(oldTotal)+' -> '+money(x.total));save();purchases()}
function deletePurchase(id0){if(!can(['Admin','Manager']))return alert('Admin/Manager pekee');let x=db.purchases.find(a=>a.id===id0),p=x&&db.products.find(a=>a.id===x.productId);if(!x)return;if(p && p.stock<x.qty)return alert('Purchase haiwezi kufutwa: stock iliyobaki ni ndogo kuliko qty ya purchase.');if(!confirm('Futa purchase ya '+x.product+'? Stock na supplier balance zitarudishwa nyuma.'))return;if(p)p.stock-=x.qty;if(x.supplierId){let sp=db.suppliers.find(a=>a.id===x.supplierId);if(sp)sp.balance=Math.max(0,(sp.balance||0)-x.balance)}db.purchases=db.purchases.filter(a=>a.id!==id0);audit('DELETE_PURCHASE',x.product+' '+x.qty);save();purchases()}
function reports(){let d=filteredReportData(),rev=d.sales.reduce((a,s)=>a+saleNet(s).total,0),cost=d.sales.reduce((a,s)=>a+saleNet(s).cost,0),exp=sum(d.expenses,'amount'),refund=d.returns.reduce((a,r)=>a+Number(r.refund||0),0),products=aggregateProducts(d.sales,d.returns),net=rev-cost-exp;main.innerHTML=`<div class=pagehead><h1 class=report-title>Ripoti</h1><div><button class=light onclick=exportCSV()>Export CSV</button> <button class=light onclick=reportWhatsApp()>WhatsApp Report</button> <button class=primary onclick=exportPDFReport()>Export PDF</button></div></div><div class=card report-summary><div class=report-controls><label>Report Type<select id=reportMode onchange=reports()><option value=all> M auzo Yote / Total Information</option><option value=day>Siku Maalum</option><option value=range>Date Range</option></select></label><label class=datefield>From<input id=reportFrom type=date onchange=reports()></label><label class=datefield>To<input id=reportTo type=date onchange=reports()></label><div></div></div><h2>${reportTitle(d.f)}</h2><div class=cards><div class=card><div class=label>Total Sales</div><div class=value>${money(rev)}</div></div><div class=card><div class=label>Cost of Goods</div><div class=value>${money(cost)}</div></div><div class=card><div class=label>Expenses</div><div class=value>${money(exp)}</div></div><div class=card><div class=label>Net Profit</div><div class=value>${money(net)}</div></div></div><p class=muted