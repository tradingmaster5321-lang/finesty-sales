atsapp(lines.join('\n'),phone)}
function csvParse(text){let rows=[],row=[],cur='',q=false;for(let i=0;i<text.length;i++){let ch=text[i];if(ch==='"'){if(q&&text[i+1]==='"'){cur+='"';i++}else q=!q}else if(ch===','&&!q){row.push(cur);cur=''}else if((ch==='\n'||ch==='\r')&&!q){if(ch==='\r'&&text[i+1]==='\n')i++;row.push(cur);cur='';if(row.some(v=>v.trim()!==''))rows.push(row);row=[]}else cur+=ch}if(cur!==''||row.length){row.push(cur);if(row.some(v=>v.trim()!==''))rows.push(row)}return rows}
function exportProductsTemplate(){let rows=[['name','sku','baseUnit','packUnit','packSize','buy','retail','wholesale','stock','min'],...db.products.map(p=>[p.name,p.sku,p.baseUnit,p.packUnit,p.packSize,p.buy,p.retail,p.wholesale,p.stock,p.min])];let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replace(/"/g,'""')+'"').join(',')).join('\n');let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'}));a.download='finesty_products_template.csv';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
function importProducts(e){if(!can(['Admin','Manager']))return alert('Admin/Manager pekee');let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>{let rows=csvParse(r.result);if(rows.length<2)return alert('CSV haina bidhaa');let h=rows[0].map(x=>x.trim().toLowerCase());let idx=(...names)=>{for(let n of names){let i=h.indexOf(n.toLowerCase());if(i>=0)return i}return -1};let c={sku:idx('sku','sku/barcode','barcode'),name:idx('product','name','jina'),unit:idx('unit','baseunit','base unit'),qty:idx('quantity','stock','opening stock'),buy:idx('cost price/unit','cost/unit','buy','cost'),retail:idx('retail price/unit','retail'),wholesale:idx('wholesale price/unit','wholesale'),packUnit:idx('packunit','pack unit'),packSize:idx('packsize','pack size'),min:idx('min','minimum stock')};let legacy=!['name','product'].some(n=>h.includes(n));if(legacy){c.name=0;c.sku=1;c.unit=2;c.packUnit=3;c.packSize=4;c.buy=5;c.retail=6;c.wholesale=7;c.qty=10;c.min=11}if(c.name<0)return alert('CSV ha column ya Product/Name');let batch={id:id(),date:new Date().toISOString(),fileName:f.name,count:0,stockValue:0};let count=0;rows.slice(1).forEach(a=>{let val=i=>i>=0?(a[i]??'').trim():'';let name=val(c.name);if(!name)return;let sku=val(c.sku);let p={id:id(),name,sku,baseUnit:val(c.unit)||'pcs',packUnit:val(c.packUnit)||'box',packSize:+val(c.packSize)||1,buy:+val(c.buy)||0,retail:+val(c.retail)||0,wholesale:+val(c.wholesale)||0,hardware:0,clothing:0,stock:+val(c.qty)||0,min:+val(c.min)||0,importBatchId:batch.id,importFileName:f.name};let old=db.products.find(x=>x.sku&&sku&&x.sku===sku);if(old){Object.assign(old,p,{id:old.id,importBatchId:batch.id,importFileName:f.name});batch.stockValue+=Number(old.stock||0)*Number(old.buy||0)}else{db.products.push(p);batch.stockValue+=p.stock*p.buy}count++});batch.count=count;db.importBatches.push(batch);audit('IMPORT_PRODUCTS',f.name+' | batch '+batch.id+' | '+count+' bidhaa | '+money(batch.stockValue));save();toast(count+' bidhaa zimeingizwa kutoka '+f.name);go('products');e.target.value=''};r.readAsText(f)}
function importBatches(){if(!can(['Admin','Manager']))return alert('Admin/Manager pekee');let batches=db.importBatches.slice().sort(txSort);main.innerHTML=`<div class=pagehead><h1>Imported Files / Batches</h1><button class=light onclick=go('products')>← Bidhaa</button></div><div class=card><p class=muted>Futa file lote kwa mara moja. Mfumo utazuia kufuta batch ambayo bidhaa zake tayari zimetumika kwenye mauzo, manunuzi au returns ili historia isiharibike.</p><div class=scroll><table><tr><th>Tarehe</th><th>File</th><th>Bidhaa</th><th>Stock Value wakati wa import</th><th>Action</th></tr>${batches.map(b=>`<tr><td>${fmtDate(b.date)}</td><td><b>${esc(b.fileName)}</b></td><td>${b.count}</td><td>${money(b.stockValue)}</td><td><button class=danger onclick="deleteImportBatch('${b.id}')">Futa Batch</button></td></tr>`).join('')||'<tr><td colspan=5>Hakuna imported batches.</td></tr>'}</table></div></div>`}
function deleteImportBatch(bid){if(!can(['Admin','Manager']))return alert('Admin/Manager pekee');let b=db.importBatches.find(x=>x.id===bid);if(!b)return;let ps=db.products.filter(p=>p.importBatchId===bid);let used=ps.filter(p=>db.sales.some(s=>s.items?.some(i=>i.id===p.id))||db.purchases.some(x=>x.productId===p.id)||db.returns.some(x=>x.productId===p.id));if(used.length)return alert('Batch hii haiwezi kufutwa lote kwa sababu bidhaa '+used.map(x=>x.name).slice(0,5).join(', ')+' tayari zimetumika kwenye transactions. Hifadhi historia kwanza.');if(!confirm('Futa imported file '+b.fileName+' yenye bidhaa '+ps.length+'? Hii itafuta bidhaa zote za batch hii.'))return;db.products=db.products.filter(p=>p.importBatchId!==bid);db.importBatches=db.importBatches.filter(x=>x.id!==bid);audit('DELETE_IMPORT_BATCH',b.fileName+' | '+ps.length+' bidhaa');save();toast('Imported batch imefutwa');importBatches()}
const DEV_SECRET='FINSTY-V1-PRO-DEV-2026';function codeCheck(raw){let a=String(raw||'').toUpperCase().replace(/[^A-Z0-9]/g,'');let sum=0;for(let i=0;i<a.length;i++)sum=(sum+a.charCodeAt(i)*(i+17))%1000003;return String(sum%997).padStart(3,'0')}function validActivationCode(code){let m=String(code||'').toUpperCase().match(/^FSP-([A-Z0-9]{4})-([A-Z0-9]{4})-([A-Z0-9]{4})-([0-9]{3})$/);if(!m)return false;return codeCheck(m[1]+m[2]+m[3]+DEV_SECRET)===m[4]}function activation(){document.getElementById('app').innerHTML=`<div class=login><div class=loginbox><div class=brand>Finesty Sales V1 Pro</div><h2>Activation ya Mfumo</h2><p class=muted>Ingiza Activation Code uliyopewa na Finesty Developer. Hii inahitajika mara ya kwanza tu.</p><input id=actCode placeholder="FSP-XXXX-XXXX-XXXX-000" style="text-transform:uppercase"><br><br><input id=actBiz placeholder="Jina la biashara"><br><br><button class=primary style="width:100%" onclick=activateSystem()>Activate System</button><p class=mini>Baada ya activation, Admin ataingia kwa PIN yake na anaweza kuibadilisha.</p></div></div>`;actCode.focus()}function activateSystem(){let c=actCode.value.trim().toUpperCase();if(!validActivationCode(c))return alert('Activation Code si sahihi. Wasiliana na Finesty Developer.');db.license={activated:true,code:c,activatedAt:new Date().toISOString(),business:actBiz.value.trim()};audit('SYSTEM_ACTIVATED',actBiz.value.trim());save();toast('Mfumo ume-activate');app()}function app(){if(!db.license?.activated){activation();return}if(!user){login();return}document.getElementById('app').innerHTML=`<header><div class=brand-wrap><div><div class=brand>Finesty Sales V1 Pro</div><div class=sub>OFFLINE • ${esc(db.settings.shop)}</div></div></div><div class=header-right><div class=header-top><button class=header-logout onclick=logout()>Logout</button></div><div class=header-meta><span id=liveClock class=live-time>--:--:--</span> • ${esc(user.name)} • ${esc(user.role)}</div></div></header><div class=layout><aside id=nav></aside><main id=main></main></div>`;nav();go(page)}function login(){d