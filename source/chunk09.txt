>Returns/Refunds: <b>${money(refund)}</b> • Products sold: <b>${products.length}</b> • Transactions: <b>${d.sales.length}</b></p></div><div class=card report-section><h2>Bidhaa Zote — Sales Details</h2>${products.map(p=>`<div class="card report-product" style="margin:10px 0"><div class=between><div class=report-product-name>${esc(p.name)}</div><div class=report-total>${money(p.profit)} Profit</div></div><p class=mini>SKU: ${esc(p.sku||'-')} • Unit: ${esc(p.unit||'-')}</p><table><tr><th>Qty Sold (net)</th><th>Sales</th><th>Returns</th><th>Net Sales</th><th>Net Cost</th><th>Profit</th></tr><tr><td>${p.qty}</td><td>${money(p.sales)}</td><td>${money(p.refund)}</td><td>${money(p.netSales)}</td><td>${money(p.netCost)}</td><td>${money(p.profit)}</td></tr></table></div>`).join('')||'<p class=muted>Hakuna bidhaa kwenye kipindi hiki.</p>'}</div><div class=grid2 report-section><div class=card><h2>Sales Transactions</h2><div class=scroll><table><tr><th>Date</th><th>Invoice</th><th>Cashier</th><th>Customer</th><th>Payment</th><th>Net Total</th><th>Profit</th></tr>${reportRows(d)}</table></div></div><div class=card><h2>Business Totals</h2><p>Sales: <b>${money(rev)}</b></p><p>COGS: <b>${money(cost)}</b></p><p>Returns/Refunds: <b>${money(refund)}</b></p><p>Expenses: <b>${money(exp)}</b></p><p class=report-total>Net Profit: ${money(net)}</p><p>Customer outstanding balance: <b>${money(sum(db.customers,'balance'))}</b></p><p>Owner / Supplier outstanding balance: <b>${money(ownerDebt())}</b></p><p>Present Capital (Current Stock Cost): <b>${money(presentCapital())}</b></p></div></div>`;let mode=document.getElementById('reportMode');if(mode)mode.value=d.f.mode;let rf=document.getElementById('reportFrom'),rt=document.getElementById('reportTo');if(rf)rf.value=d.f.from;if(rt)rt.value=d.f.to;document.querySelectorAll('.report-controls .datefield').forEach(x=>x.classList.toggle('report-hidden',d.f.mode==='all'));}
function buildReportText(){let d=filteredReportData(),products=aggregateProducts(d.sales,d.returns),rev=d.sales.reduce((a,s)=>a+saleNet(s).total,0),cost=d.sales.reduce((a,s)=>a+saleNet(s).cost,0),exp=sum(d.expenses,'amount'),refund=d.returns.reduce((a,r)=>a+Number(r.refund||0),0),lines=[`⬛🟨 *${db.settings.shop} — FINESTY SALES V1 PRO* 🟨⬛`,`🟨 *${reportTitle(d.f)}*`,`Generated: ${fmtDate(new Date().toISOString())}`,``,`*PRODUCT SALES*`];products.forEach(p=>lines.push(`
*${p.name}*`,`SKU: ${p.sku||'-'} | Unit: ${p.unit||'-'}`,`Qty: ${p.qty}`,`Sales: ${money(p.sales)}`,`Returns: ${money(p.refund)}`,`Net Sales: ${money(p.netSales)}`,`Net Cost: ${money(p.netCost)}`,`Profit: ${money(p.profit)}`));lines.push(`
*TRANSACTIONS*`);d.sales.forEach(s=>{let n=saleNet(s);lines.push(`
${fmtDate(s.date)} | ${s.invoice}`,`Cashier: ${s.userName||s.user}`,`Customer: ${s.customerName||'Walk-in'} | Payment: ${s.payment}`,`Discount: ${money(s.discount||0)}`,...s.items.map(i=>`- ${i.name} x ${i.qty} @ ${money(i.price)} = ${money(i.qty*i.price)}`),`Net Total: ${money(n.total)} | Profit: ${money(n.total-n.cost)}`)});if(d.returns.length){lines.push(`
*RETURNS*`);d.returns.forEach(r=>lines.push(`${r.date.slice(0,16).replace('T',' ')} | ${r.invoice} | ${r.product} x${r.qty} | Refund ${money(r.refund)}`))}lines.push(`
⬛🟨 *TOTALS* 🟨⬛`,`Sales: ${money(rev)}`,`COGS: ${money(cost)}`,`Returns/Refunds: ${money(refund)}`,`Expenses: ${money(exp)}`,`*NET PROFIT: ${money(rev-cost-exp)}*`,`Customer Debt (current): ${money(sum(db.customers,'balance'))}`);return lines.join('\n')}
function reportWhatsApp(){let phone=prompt('Weka namba ya WhatsApp ya kupokea report (mfano 0712345678):','');if(phone)whatsapp(buildReportText(),phone)}
function exportPDFReport(){let d=filteredReportData(),products=aggregateProducts(d.sales,d.returns),rev=d.sales.reduce((a,s)=>a+saleNet(s).total,0),cost=d.sales.reduce((a,s)=>a+saleNet(s).cost,0),exp=sum(d.expenses,'amount'),refund=d.returns.reduce((a,r)=>a+Number(r.refund||0),0),w=window.open('','_blank');if(!w){alert('Ruhusu pop-up kwa PDF');return}w.document.write(`<!doctype html><html><head><meta charset=utf-8><title>Finesty Sales V1 Pro Report</title><style>body{font-family:Arial,sans-serif;margin:28px;color:#090909}h1{color:#d4a017;margin:0;font-weight:950}h2{color:#6b4b00;border-left:5px solid #d4a017;padding-left:9px}.head{padding:18px;border-radius:16px;background:linear-gradient(135deg,#050505,#2b2415);border:3px solid #d4a017;color:#fff4b8}.head h1{color:#ffd84d}.head div{font-weight:700}.grid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:14px 0}.box{padding:12px;border:1px solid #d9b84a;border-radius:12px;background:#fffdf5}.v{font-weight:900;font-size:18px;color:#9a6b00}table{width:100%;border-collapse:collapse;margin:8px 0 18px}th,td{padding:8px;border-bottom:1px solid #dfcf9a;text-align:left;font-size:11px}th{background:#fff0a6;color:#6b4b00;border-bottom:2px solid #d4a017}.product{border:1px solid #d8b84d;border-left:4px solid #d4a017;border-radius:12px;padding:10px;margin:10px 0;break-inside:avoid}.profit{float:right;font-weight:900;color:#9a6b00}.foot{margin-top:24px;padding-top:12px;border-top:2px solid #d4a017}@media print{button{display:none}}</style></head><body><div class=head><h1>${esc(db.settings.shop)}</h1><div>${esc(reportTitle(d.f))}</div><div>${fmtDate(new Date().toISOString())}</div></div><div class=grid><div class=box>Sales<div class=v>${money(rev)}</div></div><div class=box>COGS<div class=v>${money(cost)}</div></div><div class=box>Expenses<div class=v>${money(exp)}</div></div><div class=box>Net Profit<div class=v>${money(rev-cost-exp)}</div></div></div><h2>Product Sales Details</h2>${products.map(p=>`<div class=product><span class=profit>${money(p.profit)} Profit</span><b>${esc(p.name)}</b><div>SKU: ${esc(p.sku||'-')} • Unit: ${esc(p.unit||'-')}</div><table><tr><th>Qty</th><th>Sales</th><th>Returns</th><th>Net Sales</th><th>Net Cost</th><th>Profit</th></tr><tr><td>${p.qty}</td><td>${money(p.sales)}</td><td>${money(p.refund)}</td><td>${money(p.netSales)}</td><td>${money(p.netCost)}</td><td>${money(p.profit)}</td></tr></table></div>`).join('')}<h2>Sales Transactions</h2><table><tr><th>Date</th><th>Invoice</th><th>Cashier</th><th>Customer</th><th>Payment</th><th>Net Sales</th><th>Profit</th></tr>${reportRows(d)}</table><h2>Final Totals</h2><table><tr><th>Total Sales</th><td>${money(rev)}</td></tr><tr><th>COGS</th><td>${money(cost)}</td></tr><tr><th>Returns / Refunds</th><td>${money(refund)}</td></tr><tr><th>Expenses</th><td>${money(exp)}</td></tr><tr><th>NET PROFIT</th><td><b>${money(rev-cost-exp)}</b></td></tr><tr><th>Current Customer Debt</th><td>${money(sum(db.customers,'balance'))}</td></tr><tr><th>Owner / Supplier Debt</th><td>${money(ownerDebt())}</td></tr><tr><th>Present Capital</th><td>${money(presentCapital())}</td></tr></table><div class=foot>Generated by Finesty Sales V1 Pro • Offline Business System</div><script>window.onload=()=>setTimeout(()=>window.print(),300)<\/script></body></html>`);w.document.close()}
function exportCSV(){let d=filter