.payment-entry label,.bank-entry label{display:block;margin-top:8px;font-weight:700;font-size:12px;color:#6b4b00}.quick-payment{border-top:4px solid #d4a017}.quick-payment h3{margin-top:0;color:#6b4b00}@media print{header,aside,.pagehead>div,.report-controls{display:none!important}main{padding:0;max-width:none}.card{box-shadow:none;border:1px solid #cfe0e6}.report-section{break-inside:avoid}.report-product{break-inside:avoid}body{background:#fffdf5}}
</style></head><body><div id="app"></div><div id="toast" class="toast"></div><script>
const KEY='finesty_business_offline_v1_pro';const seed={settings:{shop:'Finesty Shop',address:'',phone:'',tin:'',currency:'TZS',footer:'Asante kwa kununua!',paymentDetails:{phones:{'M-Pesa':'','Mixx by Yas':'','Airtel Money':'','HaloPesa':''},phoneNames:{'M-Pesa':'','Mixx by Yas':'','Airtel Money':'','HaloPesa':''},lipa:{'M-Pesa':'','Mixx by Yas':'','Airtel Money':'','HaloPesa':''},lipaNames:{'M-Pesa':'','Mixx by Yas':'','Airtel Money':'','HaloPesa':''},banks:{CRDB:{account:'',name:''},NBC:{account:'',name:''},NMB:{account:'',name:''}}}},users:[{id:'u1',name:'Administrator',username:'admin',phone:'',pin:'1234',role:'Admin',active:true}],products:[],sales:[],purchases:[],expenses:[],customers:[],suppliers:[],returns:[],audit:[],sessions:[],importBatches:[],license:{activated:false,code:'',activatedAt:'',business:''}};let db=load(),user=null,page='dashboard',cart=[]; if(!db.importBatches)db.importBatches=[]; if(!db.license)db.license={activated:false,code:'',activatedAt:'',business:''};if(!db.settings.paymentDetails)db.settings.paymentDetails=structuredClone(seed.settings.paymentDetails);else{db.settings.paymentDetails.phones={...seed.settings.paymentDetails.phones,...(db.settings.paymentDetails.phones||{})};db.settings.paymentDetails.phoneNames={...seed.settings.paymentDetails.phoneNames,...(db.settings.paymentDetails.phoneNames||{})};db.settings.paymentDetails.lipa={...seed.settings.paymentDetails.lipa,...(db.settings.paymentDetails.lipa||{})};db.settings.paymentDetails.lipaNames={...seed.settings.paymentDetails.lipaNames,...(db.settings.paymentDetails.lipaNames||{})};db.settings.paymentDetails.banks={...seed.settings.paymentDetails.banks,...(db.settings.paymentDetails.banks||{})};}function load(){try{let v=localStorage.getItem(KEY);if(v)return JSON.parse(v);let old=localStorage.getItem('finesty_business_offline_v5')||localStorage.getItem('finesty_business_offline_v3');return old?JSON.parse(old):structuredClone(seed)}catch(e){return structuredClone(seed)}}function save(){localStorage.setItem(KEY,JSON.stringify(db))}function id(){return Date.now().toString(36)+Math.random().toString(36).slice(2,7)}function tzNow(){return new Date().toLocaleString('en-CA',{timeZone:'Africa/Dar_es_Salaam',hour12:false}).replace(',','')}function tzDate(){return new Intl.DateTimeFormat('en-CA',{timeZone:'Africa/Dar_es_Salaam',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date())}function fmtDate(v,withTime=true){if(!v)return '-';let d=new Date(v);return new Intl.DateTimeFormat('en-GB',{timeZone:'Africa/Dar_es_Salaam',year:'numeric',month:'2-digit',day:'2-digit',hour:withTime?'2-digit':undefined,minute:withTime?'2-digit':undefined,second:withTime?'2-digit':undefined,hour12:false}).format(d).replace(',','')}function txSort(a,b){return new Date(b.date)-new Date(a.date)}function money(n){return Number(n||0).toLocaleString()+' '+db.settings.currency}function presentCapital(){return db.products.reduce((a,p)=>a+(Number(p.stock)||0)*(Number(p.buy??p.cost)||0),0)}function ownerDebt(){return sum(db.suppliers,'balance')}function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}function toast(s){let x=document.getElementById('toast');x.textContent=s;x.style.display='block';setTimeout(()=>x.style.display='none',2200)}function audit(action,detail=''){db.audit.push({id:id(),date:new Date().toISOString(),user:user?.username||'system',action,detail});save()}function can(roles){return roles.includes(user?.role)}function waNumber(phone){let n=String(phone||'').replace(/\D/g,'');if(n.startsWith('0'))n='255'+n.slice(1);if(n.startsWith('255'))return n;return n}
function whatsapp(message,phone){let n=waNumber(phone);if(!n)return alert('Namba ya WhatsApp haijawekwa');window.open('https://wa.me/'+n+'?text='+encodeURIComponent(message),'_blank')}
function saleWhatsApp(s){let c=db.customers.find(x=>x.id===s.customerId);let phone=c?.phone||'';let lines=[`*${db.settings.shop}*`,`Invoice: ${s.invoice}`,`Tarehe: ${fmtDate(s.date)}`,`Mteja: ${s.customerName||'Walk-in'}`,`Malipo: ${s.payment}`,'',...s.items.map(i=>`${i.name} x ${i.qty} = ${money(i.qty*i.price)}`),'',`*Jumla: ${money(s.total)}*`];whatsapp(lines.join('\n'),phone)}
function customerWhatsApp(cid){let c=db.customers.find(x=>x.id===cid);if(!c)return;let msg=`Habari ${c.name},\n\nTunawasiliana kutoka *${db.settings.shop}*.\nAsante kwa kuwa mteja wetu.`;whatsapp(msg,c.phone)}
function saleNet(s){if(s.voided)return {refund:0,returnedCost:0,total:0,cost:0};let rr=db.returns.filter(r=>r.invoice===s.invoice);let refund=rr.reduce((a,r)=>a+Number(r.refund||0),0);let returnedCost=rr.reduce((a,r)=>a+Number(r.cost||0),0);return {refund,returnedCost,total:Math.max(0,Number(s.total||0)-refund),cost:Math.max(0,Number(s.cost||0)-returnedCost)}}function reportWhatsApp(){let totals=db.sales.reduce((a,s)=>{let n=saleNet(s);a.rev+=n.total;a.cost+=n.cost;return a},{rev:0,cost:0}),exp=sum(db.expenses,'amount'),debt=sum(db.customers,'balance'),lines=[`*${db.settings.shop} — FULL TRANSACTION REPORT*`,`Generated: ${fmtDate(new Date().toISOString())}`,``,`*SUMMARY*`,`Sales: ${money(totals.rev)}`,`Cost of Goods: ${money(totals.cost)}`,`Expenses: ${money(exp)}`,`*Net Profit: ${money(totals.rev-totals.cost-exp)}*`,`Customer Debt: ${money(debt)}`,`Owner/Supplier Debt: ${money(ownerDebt())}`,`Present Capital: ${money(presentCapital())}`,``,`*ALL SALES TRANSACTIONS*`];db.sales.slice().sort(txSort).forEach(s=>{let n=saleNet(s);lines.push(`\nInvoice: ${s.invoice}`,`Tarehe: ${fmtDate(s.date)}`,`Cashier: ${s.userName||s.user||''}`,`Customer: ${s.customerName||'Walk-in'}`,`Customer Type: ${s.customerType||''}`,`Payment: ${s.payment}`,`Discount: ${money(s.discount||0)}`,`Items:`);s.items.forEach(i=>lines.push(`- ${i.name} x ${i.qty} @ ${money(i.price)} = ${money(i.qty*i.price)}`));lines.push(`Original Total: ${money(s.total)}`,`Returns/Refunds: ${money(n.refund)}`,`Net Sales: ${money(n.total)}`,`Net Cost: ${money(n.cost)}`,`Net Profit: ${money(n.total-n.cost)}`)});if(db.returns.length){lines.push(`\n*ALL RETURNS / REFUNDS*`);db.returns.slice().sort(txSort).forEach(r=>lines.push(`\nInvoice: ${r.invoice}`,`Tarehe: ${r.date.slice(0,16).replace('T',' ')}`,`User: ${r.user}`,`Product: ${r.product}`,`Qty: ${r.qty}`,`Refund/Value: ${money(r.refund)}`,`Method: ${r.payment}`,`Reason: ${r.reason||'-'}`))}let phone=prompt('Weka namba ya WhatsApp ya kupokea report (mfano 0712345678):','');if(phone)wh