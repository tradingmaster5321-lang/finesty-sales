ton class="primary" style="width:100%" onclick=completeSale()>Complete Sale & Receipt</button></div></div>`;renderPOSProducts();renderCart()}function renderPOS(){pos()}function renderPOSProducts(){let q=(posSearch?.value||'').toLowerCase(),type=customerType?.value||'Retail';posProducts.innerHTML=db.products.filter(p=>p.stock>0&&(p.name+' '+p.sku).toLowerCase().includes(q)).slice(0,60).map(p=>`<div class="between" style="padding:10px 0;border-bottom:1px solid #eee"><div><b>${esc(p.name)}</b><br><span class=muted>${esc(p.sku)} • ${p.stock} ${esc(p.baseUnit)}</span></div><div class=right><b>${money(priceFor(p,type))}</b><br><button onclick="addCart('${p.id}')">Add</button></div></div>`).join('')||'<p class=muted>Hakuna bidhaa.</p>'}function priceFor(p,type){return type==='Wholesale'?p.wholesale:p.retail}function addCart(pid){let p=db.products.find(x=>x.id===pid),type=customerType.value,it=cart.find(x=>x.id===pid);if(!p)return;if(it){if(it.qty>=p.stock)return alert('Stock haitoshi');it.qty++}else cart.push({id:p.id,name:p.name,qty:1,price:priceFor(p,type),cost:p.buy,unit:p.baseUnit});renderCart()}function renderCart(){let t=Math.max(0,sum(cart.map(i=>({x:i.qty*i.price})),'x')-(+discount?.value||0));cartTotal.textContent=money(t);let paid=Math.max(0,+amountPaid?.value||0),bal=Math.max(0,t-paid),chg=Math.max(0,paid-t);if(cartBalance)cartBalance.textContent=money(bal);if(cartChange)cartChange.textContent=money(chg);cartBox.innerHTML=cart.map((i,n)=>`<div class=between><div>${esc(i.name)} × ${i.qty}</div><div>${money(i.qty*i.price)} <button class=light onclick=cart.splice(${n},1);renderCart()>×</button></div></div>`).join('')||'<p class=muted>Cart iko wazi.</p>'}
function completeSale(){if(!cart.length)return alert('Cart iko wazi');let disc=Math.max(0,+discount.value||0),total=Math.max(0,sum(cart.map(i=>({x:i.qty*i.price})),'x')-disc),cost=sum(cart.map(i=>({x:i.qty*i.cost})),'x'),cid=saleCustomer.value,customer=db.customers.find(c=>c.id===cid),paid=Math.min(total,Math.max(0,+amountPaid.value||0)),change=Math.max(0,(+amountPaid.value||0)-total),balance=Math.max(0,total-paid);if(balance>0&&!cid)return alert('Kwa sale yenye balance/debt chagua Customer');if(payment.value==='Credit'&&!cid)return alert('Chagua customer kwa mauzo ya deni');for(let i of cart){let p=db.products.find(x=>x.id===i.id);if(!p||i.qty>p.stock)return alert('Stock haitoshi: '+i.name)}cart.forEach(i=>db.products.find(p=>p.id===i.id).stock-=i.qty);let s={id:id(),invoice:invoice(),date:new Date().toISOString(),user:user.username,userName:user.name,customerId:cid,customerName:customer?.name||'',customerType:customer?.type||customerType.value,payment:payment.value,total,cost,discount:disc,amountPaid:paid,balance,change,items:cart.map(x=>({...x}))};db.sales.push(s);if(customer){customer.balance=(customer.balance||0)+balance;customer.transactions=customer.transactions||[];customer.transactions.push({date:s.date,type:'sale',amount:total,paid,balance,ref:s.invoice,payment:s.payment})}audit('SALE',s.invoice+' '+money(total)+' paid '+money(paid)+' balance '+money(balance));save();cart=[];printReceipt(s);if(customer?.phone&&confirm('Tuma receipt kwa WhatsApp ya mteja?'))saleWhatsApp(s);go('pos')}
function printReceipt(s){let w=window.open('','_blank');if(!w){alert('Ruhusu pop-up kwa receipt');return}w.document.write(`<!doctype html><html><head><meta charset=utf-8><title>Receipt ${esc(s.invoice)}</title><style>@page{size:80mm auto;margin:3mm}body{font-family:Arial,sans-serif;width:72mm;margin:0 auto;color:#17120a;font-size:12px;border:3px solid #d4a017;border-radius:12px;padding:8px;background:#fffdf5;box-shadow:inset 0 0 0 2px #0b0b0b}.top{border:3px solid #d4a017;border-radius:10px;padding:10px;text-align:center;background:linear-gradient(135deg,#050505,#211b0d);color:#fff}.top h2{margin:2px 0 5px;color:#ffd84d;font-size:21px;font-weight:950;letter-spacing:.5px}.shopline{font-weight:800;color:#fff4b8}.muted{color:#765f2d;font-size:10px}.line{border-top:2px dashed #d4a017;margin:9px 0}.row{display:flex;justify-content:space-between;gap:8px;margin:4px 0}.item{padding:5px 0;border-bottom:1px solid #e3c85d}.item b{font-weight:900}.total{background:linear-gradient(135deg,#fff0a6,#ffe36b);border:3px solid #d4a017;border-radius:9px;padding:8px;font-size:15px;font-weight:950;color:#3c2a00}.balance{color:#8b5e00;font-weight:950}.footer{text-align:center;margin-top:12px;padding-top:8px;border-top:3px solid #d4a017;color:#5a430c;font-size:11px;font-weight:950;line-height:1.5}.thanks{font-size:12px;color:#9a6b00;font-weight:950}.bottom-frame{margin-top:8px;border:2px solid #0b0b0b;border-top:3px solid #d4a017;padding:6px;border-radius:8px;text-align:center;font-weight:950;color:#6b4b00}</style></head><body><div class=top><h2>${esc(db.settings.shop)}</h2><div class=shopline>${esc(db.settings.address)}</div><div class=shopline>${esc(db.settings.phone)} ${db.settings.tin?'• TIN '+esc(db.settings.tin):''}</div></div><div class=line></div><div class=row><b>Receipt</b><b>${s.invoice}</b></div><div class=row><b>Tarehe/Muda</b><span>${fmtDate(s.date)}</span></div><div class=row><b>Cashier</b><span>${esc(s.userName)}</span></div><div class=row><b>Customer</b><span>${esc(s.customerName||'Walk-in')}</span></div><div class=row><b>Payment</b><span>${esc(s.payment)}</span></div><div class=line></div>${s.items.map(i=>`<div class=item><div class=row><b>${esc(i.name)}</b><b>${money(i.qty*i.price)}</b></div><div class=muted>${i.qty} × ${money(i.price)}</div></div>`).join('')}<div class=line></div><div class=row><b>Discount</b><b>${money(s.discount||0)}</b></div><div class=total><div class=row><span>TOTAL</span><span>${money(s.total)}</span></div><div class=row><span>AMOUNT PAID</span><span>${money(s.amountPaid||0)}</span></div><div class="row balance"><span>BALANCE/DEBT</span><span>${money(s.balance||0)}</span></div><div class=row><span>CHANGE</span><span>${money(s.change||0)}</span></div></div><div class=footer><div class=thanks>${esc(db.settings.footer)}</div><div>Thank you for shopping with us.</div><div class=bottom-frame>Huduma bora • Karibu tena!</div></div><script>window.onload=()=>setTimeout(()=>window.print(),250)<\/script></body></html>`);w.document.close()}
function products(){main.innerHTML=`<div class="pagehead products-head"><h1>Bidhaa & Stock</h1><div class="product-actions"><button class=light onclick=exportProductsTemplate()>Export CSV Template</button><label class="import-label"><button class=light type=button onclick=prodFile.click()>⬆ Import CSV</button><input id=prodFile type=file accept=".csv,text/csv" style="display:none" onchange=importProducts(event)></label><button class=light onclick=importBatches()>📁 Imported Files</button><button class=primary onclick=productForm()>+ Ongeza Bidhaa</button></div></div><div class=card><div class=between><h2>Product list</h2><input id=psearch class=search placeholder="Tafuta jina/SKU/barcode" oninput=renderProducts()></div><div id=ptable></div></div>`;renderProducts()}function renderProduct