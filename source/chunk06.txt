s(){let q=(psearch?.value||'').toLowerCase(),a=db.products.filter(p=>(p.name+' '+p.sku).toLowerCase().includes(q));ptable.innerHTML=`<div class=scroll><table><tr><th>Product</th><th>SKU/Barcode</th><th>Units</th><th>Buy</th><th>Retail</th><th>Wholesale</th><th>Stock</th><th>Action</th></tr>${a.map(p=>`<tr><td><b>${esc(p.name)}</b></td><td>${esc(p.sku)}</td><td>${esc(p.baseUnit)} / ${esc(p.packUnit)} (${p.packSize})</td><td>${money(p.buy)}</td><td>${money(p.retail)}</td><td>${money(p.wholesale)}</td><td>${p.stock} ${esc(p.baseUnit)} ${p.stock===0?'<span class="badge out">OUT</span>':p.stock<=p.min?'<span class="badge low">LOW</span>':'<span class="badge ok">OK</span>'}</td><td><button class=light onclick="productForm('${p.id}')">Edit</button> <button class=danger onclick="deleteProductSafe('${p.id}')">Delete</button></td></tr>`).join('')||'<tr><td colspan=8>No products yet</td></tr>'}</table></div>`}function deleteProductSafe(pid){if(!can(['Admin','Manager']))return alert('Admin/Manager pekee');let p=db.products.find(x=>x.id===pid);if(!p)return;let used=db.sales.some(s=>s.items?.some(i=>i.id===pid))||db.purchases.some(x=>x.productId===pid)||db.returns.some(x=>x.productId===pid);if(used)return alert('Bidhaa hii tayari imetumika kwenye transaction. Haiwezi kufutwa; tumia Edit/Archive ili historia ibaki.');if(confirm('Futa '+p.name+'?')){db.products=db.products.filter(x=>x.id!==pid);audit('DELETE_PRODUCT',p.name);save();products()}}function productForm(pid){let p=db.products.find(x=>x.id===pid)||{name:'',sku:'',buy:0,retail:0,wholesale:0,stock:0,min:5,baseUnit:'pcs',packUnit:'box',packSize:1};main.innerHTML=`<div class=pagehead><h1>${pid?'Edit Product':'Ongeza Bidhaa'}</h1><button class=light onclick="go('products')">Back</button></div><div class=card><div class=formgrid><label>Jina<input id=fname value="${esc(p.name)}"></label><label>SKU / Barcode<input id=fsku value="${esc(p.sku)}"></label><label>Base unit<select id=fbase>${['pcs','box','dozen','kg','litre','meter'].map(x=>`<option ${p.baseUnit===x?'selected':''}>${x}</option>`).join('')}</select></label><label>Pack unit<input id=fpack value="${esc(p.packUnit)}"></label><label>Pack size<input id=fpacksize type=number value=${p.packSize}></label><label>Cost / unit<input id=fbuy type=number value=${p.buy}></label><label>Retail<input id=fretail type=number value=${p.retail}></label><label>Wholesale<input id=fwholesale type=number value=${p.wholesale}></label><label>Opening stock<input id=fstock type=number value=${p.stock}></label><label>Minimum stock<input id=fmin type=number value=${p.min}></label></div><br><button class=primary onclick="saveProduct('${pid||''}')">Save Product</button> ${pid?`<button class=danger onclick="deleteProduct('${pid}')">Delete</button>`:''}</div>`}function saveProduct(pid){let name=fname.value.trim();if(!name)return alert('Weka jina');let o={id:pid||id(),name,sku:fsku.value.trim(),baseUnit:fbase.value,packUnit:fpack.value.trim()||'box',packSize:+fpacksize.value||1,buy:+fbuy.value||0,retail:+fretail.value||0,wholesale:+fwholesale.value||0,stock:+fstock.value||0,min:+fmin.value||0};if(pid)db.products[db.products.findIndex(p=>p.id===pid)]=o;else db.products.push(o);audit(pid?'EDIT_PRODUCT':'ADD_PRODUCT',name);save();toast('Bidhaa imehifadhiwa');go('products')}function deleteProduct(pid){if(!can(['Admin','Manager']))return alert('Huna ruhusa');if(confirm('Futa bidhaa?')){db.products=db.products.filter(p=>p.id!==pid);audit('DELETE_PRODUCT',pid);save();go('products')}}
function purchases(){main.innerHTML=`<div class=pagehead><h1>Manunuzi / Stock In</h1></div><div class=card><div class=formgrid><label>Supplier<select id=buySupplier><option value="">—</option>${db.suppliers.map(s=>`<option value=${s.id}>${esc(s.name)}</option>`).join('')}</select></label><label>Product<select id=buyProduct>${db.products.map(p=>`<option value=${p.id}>${esc(p.name)}</option>`).join('')}</select></label><label>Qty<input id=buyQty type=number min=1></label><label>Cost / unit<input id=buyCost type=number min=0></label><label>Paid / Amount Paid<input id=buyPaid type=number min=0 value=0></label><label>Invoice/reference<input id=buyRef></label></div><br><button class=primary onclick=receiveStock()>Ingiza Stock</button></div><br><div class=card><h2>Recent purchases</h2><div class=scroll><table><tr><th>Date/Time</th><th>Supplier</th><th>Product</th><th>Qty</th><th>Total</th><th>Paid</th><th>Balance</th><th>Action</th></tr>${db.purchases.slice().sort(txSort).slice(0,30).map(x=>`<tr><td>${fmtDate(x.date)}</td><td>${esc(x.supplierName||'—')}</td><td>${esc(x.product)}</td><td>${x.qty}</td><td>${money(x.total)}</td><td>${money(x.paid)}</td><td>${money(x.total-x.paid)}</td><td><button class=light onclick="editPurchase('${x.id}')">Edit</button> <button class=danger onclick="deletePurchase('${x.id}')">Delete</button></td></tr>`).join('')||'<tr><td colspan=8>No purchases</td></tr>'}</table></div></div>`}function receiveStock(){let p=db.products.find(x=>x.id===buyProduct.value),s=db.suppliers.find(x=>x.id===buySupplier.value),q=+buyQty.value,c=+buyCost.value,total=q*c,paid=Math.min(total,Math.max(0,+buyPaid.value||0)),balance=Math.max(0,total-paid);if(!p||q<=0||c<0)return alert('Taarifa si sahihi');if(balance>0&&!s)return alert('Chagua Supplier kwa purchase yenye deni');p.stock+=q;p.buy=c;let x={id:id(),date:new Date().toISOString(),supplierId:s?.id||'',supplierName:s?.name||'',product:p.name,productId:p.id,qty:q,cost:c,total,paid,balance,ref:buyRef.value};db.purchases.push(x);if(s){s.balance=(s.balance||0)+balance;s.transactions=s.transactions||[];s.transactions.push({date:x.date,type:'purchase',amount:total,paid,balance,ref:x.ref||('PUR-'+x.id)})}audit('PURCHASE',p.name+' x'+q+' paid '+money(paid)+' balance '+money(balance));save();toast('Stock imeongezwa; supplier debt ime-record automatically');go('purchases')}
function entityPage(key,title,type){let arr=db[key].slice().sort((a,b)=>Number(b.balance||0)-Number(a.balance||0));main.innerHTML=`<div class=pagehead><h1>${title}</h1><button class=primary onclick=entityForm('${key}','')>+ Ongeza</button></div><div class=card><div class=scroll><table><tr><th>Name</th><th>Phone</th>${key==='customers'?'<th>Type</th><th>Balance</th>':'<th>Balance</th>'}<th>Action</th></tr>${arr.map(x=>`<tr><td><b>${esc(x.name)}</b></td><td>${esc(x.phone)}</td>${key==='customers'?`<td>${esc(x.type)}</td><td>${money(x.balance)}</td>`:`<td>${money(x.balance)}</td>`}<td><button class=light onclick="entityForm('${key}','${x.id}')">Edit</button> ${key==='customers'?`<button class=light onclick="customerWhatsApp('${x.id}')">WhatsApp</button> ${x.balance>0?`<button onclick="customerPayment('${x.id}')">Pay</button>`:''} <button class=light onclick="paymentLedger('customers','${x.id}')">Ledger</button>`:`${x.balance>0?`<button onclick="supplierPayment('${x.id}')">Pay</button>`:''} <button class=light onclick="paymentLedger('suppliers','${x.id}')">Ledger</button>`}</td></tr>`).join('')||'<tr><td colspan=6>Hakuna taarifa.