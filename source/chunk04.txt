ocument.getElementById('app').innerHTML=`<div class=login><div class=loginbox><div class=brand>Finesty Sales V1 Pro</div><h2>Ingia kwenye mfumo</h2><p class=muted>Tumia PIN yako binafsi</p><input id=loginPin class=pin type=password inputmode=numeric maxlength=8 placeholder="••••" onkeydown="if(event.key==='Enter')doLogin()"><br><br><button class="primary" style="width:100%" onclick=doLogin()>Ingia</button><p class=mini>Kwa installation mpya, Admin atatumia PIN iliyowekwa wakati wa setup ya mfumo.</p></div></div>`;loginPin.focus()}function doLogin(){let p=loginPin.value,u=db.users.find(x=>x.pin===p&&x.active!==false);if(!u){alert('PIN si sahihi');return}user=u;db.sessions.push({id:id(),date:new Date().toISOString(),user:u.username});audit('LOGIN');app()}function logout(){audit('LOGOUT');user=null;cart=[];app()}function nav(){let items=[['dashboard','▦','Dashboard'],['pos','🛒','Mauzo'],['products','📦','Bidhaa & Stock'],['purchases','🧾','Manunuzi'],['customers','👥','Wateja'],['suppliers','🚚','Suppliers'],['expenses','🧮','Gharama'],['debts','🔐','Madeni'],['returns','↩️','Returns'],['reports','📊','Ripoti'],['users','🧑‍💼','Users'],['backup','💾','Backup'],['payments','💳','Payment Details'],['settings','⚙️','Settings']];items=items.filter(x=>x[0]!=='users'||can(['Admin','Manager']));document.getElementById('nav').innerHTML=items.map(x=>`<button class="${page===x[0]?'active':''}" onclick="go('${x[0]}')">${x[1]} <span class=txt>${x[2]}</span></button>`).join('')}function go(p){page=p;nav();({dashboard,pos,products,purchases,customers,suppliers,expenses,debts,returns,reports,users,backup,payments,settings}[p])()}
function dashboard(){let today=tzDate(),ss=db.sales.filter(s=>new Intl.DateTimeFormat('en-CA',{timeZone:'Africa/Dar_es_Salaam'}).format(new Date(s.date))===today),rev=ss.reduce((a,s)=>a+saleNet(s).total,0),cost=ss.reduce((a,s)=>a+saleNet(s).cost,0),exp=sum(db.expenses.filter(x=>x.date.slice(0,10)===today),'amount'),low=db.products.filter(p=>p.stock<=p.min).length,capital=presentCapital(),customerDebt=sum(db.customers,'balance'),supplierDebt=ownerDebt();main.innerHTML=`<div class=pagehead><h1>Dashboard</h1><button class=primary onclick="go('pos')">+ New Sale</button></div><div class=cards><div class=card><div class=label>Present Capital</div><div class=value>${money(capital)}</div></div><div class=card><div class=label>Sales Today</div><div class=value>${money(rev)}</div></div><div class=card><div class=label>Gross Profit</div><div class=value>${money(rev-cost)}</div></div><div class=card><div class=label>Net Profit Today</div><div class=value>${money(rev-cost-exp)}</div></div></div><br><div class=cards><div class=card><div class=label>Customer Outstanding</div><div class=value>${money(customerDebt)}</div></div><div class=card><div class=label>Owner / Supplier Outstanding</div><div class=value>${money(supplierDebt)}</div></div><div class=card><div class=label>Low Stock</div><div class=value>${low}</div></div></div><br><div class=grid2><div class=card><h2>Recent sales</h2><div class=scroll><table><tr><th>Invoice</th><th>User</th><th>Customer</th><th>Total</th><th>Action</th></tr>${db.sales.slice().sort(txSort).slice(0,10).map(s=>`<tr><td>${s.invoice}</td><td>${esc(s.userName)}</td><td>${esc(s.customerName||'Walk-in')}</td><td>${money(saleNet(s).total)}</td><td><button class=light onclick="editSale('${s.id}')">Edit</button> <button class=danger onclick="voidSale('${s.id}')">Void</button></td></tr>`).join('')||'<tr><td colspan=5>No sales yet</td></tr>'}</table></div></div><div class=card><h2>Low stock</h2>${db.products.filter(p=>p.stock<=p.min).slice(0,10).map(p=>`<p><b>${esc(p.name)}</b> — ${p.stock} ${esc(p.baseUnit)} <span class="badge ${p.stock?'low':'out'}">${p.stock?'LOW':'OUT'}</span></p>`).join('')||'<p class=muted>Stock iko salama.</p>'}</div></div>`}
function sum(a,k){return a.reduce((x,y)=>x+Number(y[k]||0),0)}function invoice(){return 'INV-'+String(db.sales.length+1).padStart(5,'0')}
function editSale(id0){if(!can(['Admin','Manager']))return alert('Admin/Manager pekee');let s=db.sales.find(a=>a.id===id0);if(!s||s.voided)return alert('Sale haipo au ime-voidiwa');let nt=+prompt('Total mpya (kwa marekebisho ya thamani)',s.total);if(!(nt>=0))return;let old=s.total,diff=nt-old;if(diff===0)return;let paid=s.amountPaid||0;s.total=nt;s.balance=Math.max(0,nt-paid);s.change=Math.max(0,paid-nt);if(s.customerId){let c=db.customers.find(x=>x.id===s.customerId);if(c)c.balance=Math.max(0,(c.balance||0)-Math.max(0,old-paid)+Math.max(0,nt-paid))}audit('EDIT_SALE',s.invoice+' '+money(old)+' -> '+money(nt));save();toast('Sale imerekebishwa');dashboard()}
function voidSale(id0){if(!can(['Admin','Manager']))return alert('Admin/Manager pekee');let s=db.sales.find(a=>a.id===id0);if(!s||s.voided)return;let relatedPay=s.customerId&&db.customers.find(c=>c.id===s.customerId)?.transactions?.some(t=>t.type==='payment'&&t.ref===s.invoice);if(relatedPay)return alert('Sale hii ina payment iliyounganishwa. Rekebisha payment kwanza.');if(!confirm('VOID sale '+s.invoice+'? Stock itarudishwa na customer debt itarekebishwa.'))return;s.items.forEach(i=>{let p=db.products.find(x=>x.id===i.id);if(p)p.stock+=i.qty});if(s.customerId&&s.balance){let c=db.customers.find(x=>x.id===s.customerId);if(c)c.balance=Math.max(0,(c.balance||0)-s.balance)}s.voided=true;s.voidedAt=new Date().toISOString();s.voidedBy=user.username;audit('VOID_SALE',s.invoice);save();toast('Sale ime-voidiwa na stock imerudishwa');dashboard()}
function pos(){main.innerHTML=`<div class=pagehead><h1>Mauzo / POS</h1><button class=light onclick="cart=[];renderPOS()">Clear</button></div><div class=grid2><div class=card><div class=row><input id=posSearch class=search placeholder="Tafuta jina, SKU au barcode..." oninput=renderPOSProducts()><select id=customerType onchange=renderPOSProducts()><option>Retail</option><option>Wholesale</option></select></div><div id=posProducts></div></div><div class=card><h2>Cart</h2><div id=cartBox></div><hr><div class=grid3><label>Customer<select id=saleCustomer><option value="">Walk-in</option>${db.customers.map(c=>`<option value=${c.id}>${esc(c.name)} — ${esc(c.type)}</option>`).join('')}</select></label><label>Payment<select id=payment onchange=renderCart()><option>Cash</option><option>M-Pesa</option><option>Airtel Money</option><option>Tigo Pesa</option><option>HaloPesa</option><option>Bank/Card</option><option>Credit</option></select></label><label>Discount<input id=discount type=number min=0 value=0 oninput=renderCart()></label></div><div class=grid3><label>Amount Paid<input id=amountPaid type=number min=0 value=0 oninput=renderCart()></label><div class=card style="padding:10px"><div class=label>Balance / Debt</div><div id=cartBalance class=report-total>${money(0)}</div></div><div class=card style="padding:10px"><div class=label>Change</div><div id=cartChange class=report-total>${money(0)}</div></div></div><br><div class=right>Total Required: <span id=cartTotal class=total>${money(0)}</span></div><br><but