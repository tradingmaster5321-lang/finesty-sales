cument.getElementById('ph'+i).value.trim();pd.phoneNames[n]=document.getElementById('phn'+i).value.trim();pd.lipa[n]=document.getElementById('lp'+i).value.trim();pd.lipaNames[n]=document.getElementById('lpn'+i).value.trim()});banks.forEach((b,i)=>{pd.banks[b].account=document.getElementById('ba'+i).value.trim();pd.banks[b].name=document.getElementById('bn'+i).value.trim()});audit('PAYMENT_DETAILS_UPDATE');save();toast('Payment details zimehifadhiwa');payments()}function settings(){let me=db.users.find(u=>u.id===user.id)||user;main.innerHTML=`<div class=pagehead><h1>Personal Information & Settings</h1><button class=primary onclick=go('payments')>💳 Payment Details</button></div><div class=grid2><div class=card><h2>Personal Information</h2><div class=formgrid"><label>Jina<input id=myname value="${esc(me.name)}"></label><label>Username<input value="${esc(me.username)}" disabled></label><label>Phone<input id=myphone value="${esc(me.phone)}"></label></div><br><button class=primary onclick=saveProfile()>Save Profile</button></div><div class=card><h2>Badili PIN</h2><label>Current PIN<input id=oldpin type=password maxlength=8></label><br><label>New PIN<input id=newpin type=password maxlength=8></label><br><label>Confirm PIN<input id=confpin type=password maxlength=8></label><br><button class=primary onclick=changePin()>Change PIN</button></div></div><br><div class=card><h2>Business Settings</h2><div class=formgrid><label>Shop name<input id=shop value="${esc(db.settings.shop)}"></label><label>Address<input id=addr value="${esc(db.settings.address)}"></label><label>Phone<input id=shopphone value="${esc(db.settings.phone)}"></label><label>TIN/VAT<input id=tin value="${esc(db.settings.tin)}"></label><label>Currency<select id=cur><option ${db.settings.currency==='TZS'?'selected':''}>TZS</option><option ${db.settings.currency==='USD'?'selected':''}>USD</option></select></label><label>Receipt footer<input id=foot value="${esc(db.settings.footer)}"></label></div><br><button class=primary onclick=saveBusinessSettings()>Save Business Settings</button></div><br><div class=card><h2>Security / Activity</h2><div class=scroll><table><tr><th>Date</th><th>User</th><th>Action</th><th>Detail</th></tr>${db.audit.slice().sort(txSort).slice(0,40).map(a=>`<tr><td>${fmtDate(a.date)}</td><td>${esc(a.user)}</td><td>${esc(a.action)}</td><td>${esc(a.detail)}</td></tr>`).join('')}</table></div></div>`}function saveProfile(){let me=db.users.find(u=>u.id===user.id);me.name=myname.value.trim();me.phone=myphone.value.trim();user=me;audit('PROFILE_UPDATE');save();toast('Profile imehifadhiwa');app()}function changePin(){let me=db.users.find(u=>u.id===user.id);if(oldpin.value!==me.pin)return alert('Current PIN si sahihi');if(newpin.value.length<4||newpin.value!==confpin.value)return alert('PIN mpya haifanani au ni fupi');me.pin=newpin.value;user=me;audit('PIN_CHANGE');save();toast('PIN imebadilishwa') }function saveBusinessSettings(){if(!can(['Admin','Manager']))return alert('Huna ruhusa');Object.assign(db.settings,{shop:shop.value.trim()||'Finesty Shop',address:addr.value.trim(),phone:shopphone.value.trim(),tin:tin.value.trim(),currency:cur.value,footer:foot.value});audit('BUSINESS_SETTINGS');save();toast('Business settings zimehifadhiwa');app()}
/* FINAL PRODUCT IMPORT FIX: supports the actual Excel template (.xlsx) and CSV, including Product Name, Category, Unit, Quantity, Purchase Price, Selling Price, Minimum Stock and Supplier. */
function importProductsText(text,fileName){if(!can(['Admin','Manager']))return alert('Admin/Manager pekee');try{let rows=csvParse(String(text||'').replace(/^\uFEFF/,''));if(rows.length<2)throw new Error('File haina rows za bidhaa');let norm=s=>String(s??'').toLowerCase().replace(/^\uFEFF/,'').trim().replace(/[()]/g,'').replace(/\s+/g,' ');let h=rows[0].map(norm);let find=(...names)=>{let ns=names.map(norm);for(let i=0;i<h.length;i++)if(ns.includes(h[i]))return i;for(let i=0;i<h.length;i++)if(ns.some(n=>h[i].includes(n)))return i;return -1};let c={name:find('product name','product','name','jina la bidhaa','jina'),category:find('category','category name','aina','kategoria'),unit:find('unit','base unit','baseunit'),qty:find('quantity','qty','stock','opening stock','idadi'),buy:find('purchase price tzs','purchase price','purchase','cost price/unit','cost/unit','buy','cost','bei ya kununua'),retail:find('selling price tzs','selling price','retail price/unit','retail','selling','bei ya kuuzia','bei ya kuuza'),wholesale:find('wholesale price/unit','wholesale price','wholesale','jumla'),min:find('minimum stock','min stock','minimum','min'),supplier:find('supplier','supplier name','msambazaji')};if(c.name<0)throw new Error('Column ya Product Name haipo');if(c.buy<0)throw new Error('Column ya Purchase Price haipo');if(c.retail<0)throw new Error('Column ya Selling Price haipo');let val=(a,i)=>i>=0?String(a[i]??'').trim():'';let num=(a,i)=>{let x=val(a,i).replace(/,/g,'').replace(/TSh|TZS/gi,'').trim();let n=Number(x);return Number.isFinite(n)?n:0};let batch={id:id(),date:new Date().toISOString(),fileName:fileName||'imported_file',count:0,stockValue:0};let count=0,errors=[];rows.slice(1).forEach((a,ri)=>{if(!a.some(x=>String(x??'').trim()!==''))return;let name=val(a,c.name);if(!name){errors.push('Row '+(ri+2)+': Product Name haipo');return}let sku=val(a,find('sku','sku/barcode','barcode'));let qty=num(a,c.qty),buy=num(a,c.buy),retail=num(a,c.retail),wholesale=c.wholesale>=0?num(a,c.wholesale):retail;let p={id:id(),name,sku,category:val(a,c.category)||'Vipodozi',baseUnit:val(a,c.unit)||'pcs',packUnit:'box',packSize:1,buy,retail,wholesale,hardware:0,clothing:0,stock:qty,min:num(a,c.min),supplier:val(a,c.supplier),importBatchId:batch.id,importFileName:fileName||'imported_file'};if(buy<0||retail<0||qty<0){errors.push('Row '+(ri+2)+': thamani hasi');return}let old=sku?db.products.find(x=>x.sku&&x.sku===sku):null;if(old){let oldStock=Number(old.stock||0);Object.assign(old,p,{id:old.id});batch.stockValue+=qty*buy}else{db.products.push(p);batch.stockValue+=qty*buy}count++});if(!count)throw new Error(errors[0]||'Hakuna bidhaa iliyoweza kuingizwa');batch.count=count;db.importBatches.push(batch);audit('IMPORT_PRODUCTS',batch.fileName+' | batch '+batch.id+' | '+count+' bidhaa | '+money(batch.stockValue));save();let msg=count+' bidhaa zimeingizwa kutoka '+batch.fileName;if(errors.length)msg+=' | '+errors.length+' row zimeachwa';toast(msg);alert(msg);go('products')}catch(err){alert('Import haijafanikiwa: '+(err.message||err))}}
function receiveNativeImport(text,fileName){importProductsText(text,fileName)}function receiveNativeImportError(msg){alert(msg||'Import haijafanikiwa')}
function importProducts(e){let f=e&&e.target&&e.target.files?e.target.files[0]:null;if(!f)return;let r=new FileReader();r.onload=()=>importProductsText(r.result,f.name);r.readAsText(f)}
function products(){main.innerHTML=`<div class="pagehead products-head"><h1>Bidhaa & Stock</h1><div class="product-actions"><button class=light onclick=exportProductsTemplate()>Export CSV Template</button><label class="import-label"><button class=primary type=button onclick="if(window.AndroidBridge&&AndroidBridge.openProductImport){AndroidBridge.openProductImport()}else{document.getElementById('prodFile').click()}">📂 Import Product File (CSV / Excel)</button><input id=prodFile type=file accept=".csv,.xlsx,.xls,text/csv,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" style="display:none" onchange=importProducts(event)></label><button class=light onclick=importBatches()>📁 Imported Files</button><button class=primary onclick=productForm()>+ Ongeza Bidhaa</button></div></div><div class=card><div class=between><h2>Product list</h2><input id=psearch class=search placeholder="Tafuta jina/SKU/barcode" oninput=renderProducts()></div><div id=ptable></div></div>`;renderProducts()}
function settings(){let me=db.users.find(u=>u.id===user.id)||user;main.innerHTML=`<div class=pagehead><h1>Personal Information & Settings</h1><button class=primary onclick=go('payments')>💳 Payment Details</button></div><div class=grid2><div class=card><h2>Personal Information</h2><div class=formgrid><label>Jina<input id=myname value="${esc(me.name)}"></label><label>Username<input value="${esc(me.username)}" disabled></label><label>Phone<input id=myphone value="${esc(me.phone)}"></label></div><br><button class=primary onclick=saveProfile()>Save Profile</button></div><div class=card><h2>Badili PIN</h2><label>Current PIN<input id=oldpin type=password maxlength=8></label><br><label>New PIN<input id=newpin type=password maxlength=8></label><br><label>Confirm PIN<input id=confpin type=password maxlength=8></label><br><button class=primary onclick=changePin()>Change PIN</button></div></div><br><div class=card><h2>Business Settings</h2><div class=formgrid><label>Shop name<input id=shop value="${esc(db.settings.shop)}"></label><label>Address<input id=addr value="${esc(db.settings.address)}"></label><label>Phone<input id=shopphone value="${esc(db.settings.phone)}"></label><label>TIN/VAT<input id=tin value="${esc(db.settings.tin)}"></label><label>Currency<select id=cur><option ${db.settings.currency==='TZS'?'selected':''}>TZS</option><option ${db.settings.currency==='USD'?'selected':''}>USD</option></select></label><label>Receipt footer<input id=foot value="${esc(db.settings.footer)}"></label></div><br><button class=primary onclick=saveBusinessSettings()>Save Business Settings</button></div><br><div class=card><h2>📱 Device Access & Permissions</h2><p class=muted>Import Product File hutumia Android File Picker na inaweza kusoma CSV au Excel (.xlsx). PDF hutumia Android Print/Save as PDF. WhatsApp hutumia WhatsApp/share action.</p><div class=row style="flex-wrap:wrap"><button class=light onclick="if(AndroidBridge&&AndroidBridge.requestPermissions)AndroidBridge.requestPermissions()">🔐 Request Device Permissions</button><button class=light onclick="go('products')">📂 Import Product File</button></div><div id=devicePermStatus class=mini style="margin-top:10px;font-weight:800">Hali ya permissions inatafutwa...</div></div><br><div class=card><h2>Security / Activity</h2><div class=scroll><table><tr><th>Date</th><th>User</th><th>Action</th><th>Detail</th></tr>${db.audit.slice().sort(txSort).slice(0,40).map(a=>`<tr><td>${fmtDate(a.date)}</td><td>${esc(a.user)}</td><td>${esc(a.action)}</td><td>${esc(a.detail)}</td></tr>`).join('')}</table></div></div>`;setTimeout(()=>{let e=document.getElementById('devicePermStatus');if(e&&window.AndroidBridge&&AndroidBridge.getPermissionStatus)e.textContent=AndroidBridge.getPermissionStatus()},150)}
app();setInterval(()=>{let e=document.getElementById('liveClock');if(e)e.textContent=new Date().toLocaleTimeString('en-GB',{timeZone:'Africa/Dar_es_Salaam',hour12:false});},1000);</script></body></html>