/td></tr>'}</table></div></div>`}function customers(){entityPage('customers','Wateja')}function suppliers(){entityPage('suppliers','Suppliers')}function entityForm(key,pid){let x=db[key].find(a=>a.id===pid)||{name:'',phone:'',address:'',type:'Retail',balance:0};main.innerHTML=`<div class=pagehead><h1>${pid?'Edit':'Ongeza'} ${key==='customers'?'Customer':'Supplier'}</h1><button class=light onclick="go('${key}')">Back</button></div><div class=card><div class=formgrid><label>Jina<input id=en value="${esc(x.name)}"></label><label>Simu<input id=ep value="${esc(x.phone)}"></label><label>Address<input id=ea value="${esc(x.address)}"></label>${key==='customers'?`<label>Customer type<select id=etype>${['Retail','Wholesale'].map(t=>`<option ${x.type===t?'selected':''}>${t}</option>`).join('')}</select></label>`:''}</div><br><button class=primary onclick="saveEntity('${key}','${pid||''}')">Save</button></div>`}function saveEntity(key,pid){let old=pid?db[key].find(x=>x.id===pid):null,o={id:pid||id(),name:en.value.trim(),phone:ep.value.trim(),address:ea.value.trim()};if(!o.name)return alert('Weka jina');if(key==='customers')o={...o,type:etype.value,balance:old?.balance||0,transactions:old?.transactions||[]};else o={...o,balance:old?.balance||0,transactions:old?.transactions||[]};if(pid)db[key][db[key].findIndex(x=>x.id===pid)]=o;else db[key].push(o);audit('SAVE_'+key,o.name);save();go(key)}function paymentLedger(key,id0){let x=db[key].find(a=>a.id===id0);if(!x)return;let pays=(x.transactions||[]).filter(t=>t.type==='payment');if(!pays.length)return alert('Hakuna malipo ya kurekebisha.');let list=pays.map((t,i)=>`${i+1}. ${fmtDate(t.date)} — ${money(t.amount)} — ${t.ref||''}`).join('\n');let n=prompt('Chagua namba ya malipo kufuta:\n'+list);if(n===null)return;let idx=+n-1,t=pays[idx];if(!t)return alert('Namba si sahihi');if(!confirm('Futa malipo '+money(t.amount)+'? Balance itaongezeka tena.'))return;x.balance=(x.balance||0)+Number(t.amount||0);x.transactions=x.transactions.filter(a=>a!==t);audit('DELETE_'+(key==='customers'?'CUSTOMER':'SUPPLIER')+'_PAYMENT',x.name+' '+money(t.amount));save();go(key)}function customerPayment(cid){let c=db.customers.find(x=>x.id===cid),a=prompt('Kiasi cha malipo (balance '+money(c.balance)+')');a=+a;if(!a||a<=0)return;if(a>c.balance)a=c.balance;c.balance-=a;c.transactions=c.transactions||[];c.transactions.push({date:new Date().toISOString(),type:'payment',amount:a,ref:'PAY-'+id()});audit('CUSTOMER_PAYMENT',c.name+' '+money(a)+' remaining '+money(c.balance));save();toast('Malipo yamehifadhiwa; balance imesasishwa');go('customers')}function supplierPayment(sid){let s=db.suppliers.find(x=>x.id===sid),a=prompt('Kiasi cha supplier payment (balance '+money(s.balance)+')');a=+a;if(!a||a<=0)return;if(a>s.balance)a=s.balance;s.balance-=a;s.transactions=s.transactions||[];s.transactions.push({date:new Date().toISOString(),type:'payment',amount:a,ref:'SUPPAY-'+id()});audit('SUPPLIER_PAYMENT',s.name+' '+money(a)+' remaining '+money(s.balance));save();toast('Supplier payment imehifadhiwa; balance imesasishwa');go('suppliers')}
function expenses(){main.innerHTML=`<div class=pagehead><h1>Gharama</h1></div><div class=card><div class=formgrid><label>Category<input id=excat placeholder="Kodi, mshahara, usafiri..."></label><label>Description<input id=exname></label><label>Amount<input id=examt type=number min=0></label></div><br><button class=primary onclick=addExpense()>Hifadhi</button></div><br><div class=card><table><tr><th>Date</th><th>Category</th><th>Description</th><th>Amount</th><th>Action</th></tr>${db.expenses.slice().sort(txSort).map(x=>`<tr><td>${fmtDate(x.date)}</td><td>${esc(x.cat)}</td><td>${esc(x.name)}</td><td>${money(x.amount)}</td><td><button class=light onclick=editExpense('${x.id}')>Edit</button> <button class=danger onclick=deleteExpense('${x.id}')>Delete</button></td></tr>`).join('')}</table></div>`}
function editExpense(id0){let x=db.expenses.find(a=>a.id===id0);if(!x)return;let a=prompt('Amount mpya',x.amount);if(a===null)return;let n=+a;if(!(n>=0))return alert('Amount si sahihi');let old=x.amount;x.amount=n;x.cat=prompt('Category',x.cat)||x.cat;x.name=prompt('Description',x.name)||x.name;audit('EDIT_EXPENSE',x.name+' '+money(old)+' -> '+money(n));save();expenses()}
function deleteExpense(id0){if(!can(['Admin','Manager']))return alert('Admin/Manager pekee');let x=db.expenses.find(a=>a.id===id0);if(!x)return;if(!confirm('Futa expense '+x.name+' ya '+money(x.amount)+'?'))return;db.expenses=db.expenses.filter(a=>a.id!==id0);audit('DELETE_EXPENSE',x.name+' '+money(x.amount));save();expenses()}
function debts(){let cs=db.customers.filter(c=>(c.balance||0)>0).sort((a,b)=>b.balance-a.balance),ss=db.suppliers.filter(s=>(s.balance||0)>0).sort((a,b)=>b.balance-a.balance);main.innerHTML=`<div class=pagehead><h1>Madeni</h1></div><div class=grid2><div class=card><h2>Wateja wanaodaiwa</h2>${cs.map(c=>`<div class=between style="padding:10px 0;border-bottom:1px solid #d4a017"><div><b>${esc(c.name)}</b><br><span class=mini>Balance: ${money(c.balance)}</span></div><button onclick=customerPayment('${c.id}')>Record payment</button></div>`).join('')||'<p class=muted>Hakuna madeni ya customer.</p>'}</div><div class=card><h2>Supplier debts</h2>${ss.map(s=>`<div class=between style="padding:10px 0;border-bottom:1px solid #d4a017"><div><b>${esc(s.name)}</b><br><span class=mini>Balance: ${money(s.balance)}</span></div><button onclick=supplierPayment('${s.id}')>Record payment</button></div>`).join('')||'<p class=muted>Hakuna supplier debt.</p>'}</div></div><br><div class=card><h2>Recent Debt Payments</h2><div class=scroll><table><tr><th>Date/Time</th><th>Account</th><th>Type</th><th>Amount</th></tr>${[...db.customers.flatMap(c=>(c.transactions||[]).filter(t=>t.type==='payment').map(t=>({...t,name:c.name,account:'Customer'}))),...db.suppliers.flatMap(s=>(s.transactions||[]).filter(t=>t.type==='payment').map(t=>({...t,name:s.name,account:'Supplier'})))].sort(txSort).slice(0,30).map(t=>`<tr><td>${fmtDate(t.date)}</td><td>${esc(t.name)}</td><td>${t.account}</td><td>${money(t.amount)}</td></tr>`).join('')||'<tr><td colspan=4>No payments</td></tr>'}</table></div></div>`}
function deleteReturn(id0){if(!can(['Admin','Manager']))return alert('Admin/Manager pekee');let r=db.returns.find(x=>x.id===id0);if(!r)return;let p=db.products.find(x=>x.id===r.productId);if(!p||p.stock<r.qty)return alert('Return haiwezi kufutwa: stock iliyorejeshwa tayari imetumika.');if(!confirm('Futa return ya '+r.product+' x'+r.qty+'?'))return;p.stock-=r.qty;let s=db.sales.find(x=>x.invoice===r.invoice);if(s?.payment==='Credit'&&s.customerId){let c=db.customers.find(x=>x.id===s.customerId);if(c)c.balance=(c.balance||0)+r.refund}db.returns=db.returns.filter(x=>x.id!==id0);audit('DELETE_RETURN',r.invoice+' '+r.product+' x'+r.qty);save();returns()}function returns(){main.innerHTML=`<div class=pagehead><h1>Returns / Refunds</h1></div><div class=card><div class=formgrid><label>Invoice<input id=retInv></label><label>Product<select id=retProduct>${db.products.map(p=>`<option value=${p.id}>${esc(p.name)}</option>`).join('')}</select></label><label>Qty<input id=retQty type=number min=1 value=1></label><label>Reason<input id=retReason></label><label>Refund method<select id=retPay><option>Cash</option><option>Original payment</option><option>Exchange</option></select></label></div><br><button class=primary onclick=processReturn()>Process Return</button></div><br><div class=card><h2>Return history</h2><table><tr><th>Date</th><th>Invoice</th><th>Product</th><th>Qty</th><th>Refund</th></tr>${db.returns.slice().sort(txSort).map(r=>`<tr><td>${fmtDate(r.date)}</td><td>${r.invoice}</td><td>${esc(r.product)}</td><td>${r.qty}</td><td>${money(r.refund)}</td></tr>`).join('')}</table></div>`}function processReturn(){let s=db.sales.find(x=>x.invoice===retInv.value.trim());if(!s)return alert('Invoice haijapatikana');let p=db.products.find(x=>x.id===retProduct.value),line=s.items.find(i=>i.id===p?.id);let q=+retQty.value;if(!p||!line||q<=0)return alert('Bidhaa/quantity si sahihi');let already=db.returns.filter(r=>r.invoice===s.invoice&&r.productId===p.id).reduce((a,r)=>a+Number(r.qty||0),0),remaining=Math.max(0,Number(line.qty||0)-already);if(q>remaining)return alert('Quantity ya return imezidi kiasi kilichouzwa. Iliyobaki kurudishwa: '+remaining);let refund=line.price*q,cost=line.cost*q;p.stock+=q;db.returns.push({id:id(),date:new Date().toISOString(),invoice:s.invoice,product:p.name,productId:p.id,qty:q,refund,cost,reason:retReason.value,payment:retPay.value,user:user.username});if(s.payment==='Credit'&&s.customerId){let c=db.customers.find(x=>x.id===s.customerId);if(c){c.balance=Math.max(0,(c.balance||0)-refund);c.transactions=c.transactions||[];c.transactions.push({date:new Date().toISOString(),type:'return_refund',amount:refund,ref:s.invoice})}}audit('RETURN',s.invoice+' '+p.name+' x'+q+' refund '+money(refund));save();toast('Return imehifadhiwa: stock, sales na profit vimesasishwa');go('returns')}