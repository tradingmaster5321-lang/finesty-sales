edReportData(),rows=[['Product','Date','Invoice','Cashier','Customer','Payment','Qty','Unit Price','Line Sales','Original Total','Refunds','Net Sales','Net Cost','Net Profit'],...d.sales.flatMap(s=>s.items.map(i=>{let n=saleNet(s);return [i.name,s.date,s.invoice,s.userName,s.customerName,s.payment,i.qty,i.price,i.qty*i.price,s.total,n.refund,n.total,n.cost,n.total-n.cost]}))];let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replace(/"/g,'""')+'"').join(',')).join('\n'),a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'}));a.download='finesty_sales_report_'+(d.f.mode==='all'?'all':d.f.from+(d.f.to?('_to_'+d.f.to):''))+'.csv';a.click()}
function users(){main.innerHTML=`<div class=pagehead><h1>Users & Permissions</h1><button class=primary onclick=userForm()>+ Add User</button></div><div class=card><table><tr><th>Name</th><th>Username</th><th>Phone</th><th>Role</th><th>Status</th><th>Action</th></tr>${db.users.map(u=>`<tr><td>${esc(u.name)}</td><td>${esc(u.username)}</td><td>${esc(u.phone)}</td><td>${esc(u.role)}</td><td>${u.active!==false?'Active':'Disabled'}</td><td><button class=light onclick="userForm('${u.id}')">Edit</button></td></tr>`).join('')}</table></div>`}function userForm(uid){let u=db.users.find(x=>x.id===uid)||{name:'',username:'',phone:'',pin:'',role:'Cashier',active:true};main.innerHTML=`<div class=pagehead><h1>${uid?'Edit User':'Add User'}</h1></div><div class=card><div class=formgrid><label>Full name<input id=un value="${esc(u.name)}"></label><label>Username<input id=uu value="${esc(u.username)}"></label><label>Phone<input id=up value="${esc(u.phone)}"></label><label>PIN ${uid?'(acha wazi ili ibaki)':''}<input id=upin type=password maxlength=8 value=""></label><label>Role<select id=urole>${['Admin','Manager','Cashier'].map(r=>`<option ${u.role===r?'selected':''}>${r}</option>`).join('')}</select></label><label>Status<select id=uactive><option value=true ${u.active!==false?'selected':''}>Active</option><option value=false ${u.active===false?'selected':''}>Disabled</option></select></label></div><br><button class=primary onclick="saveUser('${uid||''}')">Save User</button></div>`}function saveUser(uid){if(!can(['Admin']))return alert('Admin pekee ndiye anaweza kudhibiti users');let old=db.users.find(x=>x.id===uid),o={id:uid||id(),name:un.value.trim(),username:uu.value.trim(),phone:up.value.trim(),pin:upin.value?upin.value:(old?.pin||''),role:urole.value,active:uactive.value==='true'};if(!o.name||!o.username||!o.pin)return alert('Jaza taarifa zote na PIN');if(db.users.some(x=>x.username===o.username&&x.id!==o.id))return alert('Username tayari ipo');if(uid)db.users[db.users.findIndex(x=>x.id===uid)]=o;else db.users.push(o);audit('SAVE_USER',o.username);save();go('users')}
function backup(){main.innerHTML=`<div class=pagehead><h1>Backup & Restore</h1></div><div class=grid2><div class=card><h2>Backup ya offline</h2><p class=muted>Data yote ya biashara itapakuliwa kama JSON.</p><button class=primary onclick=downloadBackup()>Download Backup</button></div><div class=card><h2>Restore</h2><input type=file accept=.json onchange=restoreBackup(event)><p class=mini>Restore inabadilisha database ya sasa. Tumia backup sahihi.</p></div></div>`}function downloadBackup(){audit('BACKUP');let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(db,null,2)],{type:'application/json'}));a.download='finesty_backup_'+new Date().toISOString().slice(0,10)+'.json';a.click()}function restoreBackup(e){if(!can(['Admin']))return alert('Admin pekee');let r=new FileReader();r.onload=()=>{try{let x=JSON.parse(r.result);if(!x.settings||!x.users||!x.products||!x.sales)throw Error();db=x;user=db.users.find(u=>u.username===user.username)||db.users[0];save();toast('Backup imerejeshwa');app()}catch(_){alert('Backup si sahihi')}};r.readAsText(e.target.files[0])}
function payments(){let pd=db.settings.paymentDetails||seed.settings.paymentDetails,networks=['M-Pesa','Mixx by Yas','Airtel Money','HaloPesa'];main.innerHTML=`<div class=pagehead><div><h1>Payment Details</h1><p class=muted>Jaza namba, jina la akaunti na taarifa za malipo. Taarifa hizi zitaonekana kwenye Quick View.</p></div></div><div class=grid2><div class=card payment-card><h2>📱 Namba za Simu</h2><p class=mini muted>Namba ya simu + Account Name kwa kila mtandao.</p>${networks.map((n,i)=>`<div class=payment-entry><div class=payment-entry-title><span class=payment-icon>${i===0?'📱':i===1?'📡':i===2?'☎️':'📲'}</span><b>${n}</b></div><label>Namba ya Simu<input id=ph${i} value="${esc(pd.phones[n]||'')}" placeholder="Namba ya simu" inputmode="tel"></label><label>Account Name<input id=phn${i} value="${esc(pd.phoneNames?.[n]||'')}" placeholder="Jina la akaunti"></label></div>`).join('')}</div><div class=card payment-card><h2>💳 Lipa Namba</h2><p class=mini muted>Lipa Namba + Account Name kwa kila mtandao.</p>${networks.map((n,i)=>`<div class=payment-entry><div class=payment-entry-title><span class=payment-icon>${i===0?'💳':i===1?'🔢':i===2?'🏷️':'💠'}</span><b>${n}</b></div><label>Lipa Namba<input id=lp${i} value="${esc(pd.lipa[n]||'')}" placeholder="Lipa Namba" inputmode="numeric"></label><label>Account Name<input id=lpn${i} value="${esc(pd.lipaNames?.[n]||'')}" placeholder="Jina la akaunti"></label></div>`).join('')}</div></div><br><div class=card payment-card><h2>🏦 Bank Accounts</h2><p class=mini muted>Account Number + Account Name kwa kila benki.</p><div class=formgrid>${['CRDB','NBC','NMB'].map((b,i)=>`<div class=card bank-entry><div class=payment-entry-title><span class=payment-icon>${b==='CRDB'?'🏦':b==='NBC'?'🏛️':'💼'}</span><b>${b}</b></div><label>Account Number<input id="ba${i}" value="${esc(pd.banks[b]?.account||'')}" placeholder="Account Number" inputmode="numeric"></label><label>Account Name<input id="bn${i}" value="${esc(pd.banks[b]?.name||'')}" placeholder="Account Name"></label></div>`).join('')}</div></div><br><div class=card payment-card><h2>👁️ Quick View</h2><div class=grid2>${networks.map(n=>`<div class=card quick-payment><h3>📱 ${n} — Simu</h3><p><b>Namba:</b> ${esc(pd.phones[n]||'—')}</p><p><b>Account Name:</b> ${esc(pd.phoneNames?.[n]||'—')}</p></div>`).join('')}</div><div class=grid2>${networks.map(n=>`<div class=card quick-payment><h3>💳 ${n} — Lipa Namba</h3><p><b>Lipa Namba:</b> ${esc(pd.lipa[n]||'—')}</p><p><b>Account Name:</b> ${esc(pd.lipaNames?.[n]||'—')}</p></div>`).join('')}</div><div class=grid3>${['CRDB','NBC','NMB'].map(b=>`<div class=card quick-payment><h3>🏦 ${b}</h3><p><b>Account Number:</b> ${esc(pd.banks[b]?.account||'—')}</p><p><b>Account Name:</b> ${esc(pd.banks[b]?.name||'—')}</p></div>`).join('')}</div></div><button class=primary onclick=savePaymentDetails()>💾 Save Payment Details</button>`}
function savePaymentDetails(){if(!can(['Admin','Manager']))return alert('Huna ruhusa');let networks=['M-Pesa','Mixx by Yas','Airtel Money','HaloPesa'],banks=['CRDB','NBC','NMB'],pd=db.settings.paymentDetails;networks.forEach((n,i)=>{pd.phones[n]=do